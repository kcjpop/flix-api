---
import Markdown from './Markdown.astro'

const doc = Astro.props.doc?.join('\n')
---

<style>
  .content {
    color: var(--text-2);
  }
</style>

{
  doc && (
    <div class="content">
      <Markdown doc={doc} />
    </div>
  )
}
